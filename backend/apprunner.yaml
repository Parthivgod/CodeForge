version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building backend for App Runner"
run:
  runtime-version: latest
  command: uvicorn main:app --host 0.0.0.0 --port 8000 --workers 1
  network:
    port: 8000
    env: PORT
  env:
    - name: PORT
      value: "8000"